pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     sh'echo python --version'
                 }
                 }
         }
}
