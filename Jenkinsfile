pipeline {
         agent{docker {image'python:3.8.3'}}
         stages {
                 stage('One') {
                 steps {
                     sh'python --version'
                 }
                 }
         }
}
