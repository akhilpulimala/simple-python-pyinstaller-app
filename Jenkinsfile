pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo' python --version'
                 }
                 }
         }
}
