pipeline {
         agent{docker {image'python:3.5.1'}}
         stages {
                 stage('One') {
                 steps {
                     sh'python --version'
                 }
                 }
         }
}
