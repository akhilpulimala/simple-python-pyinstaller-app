pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo' python -m sources/calc.py'
                 }
                 }
         }
}
